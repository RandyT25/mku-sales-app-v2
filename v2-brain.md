# mku-sales-app-v2 — Living Architecture & Decisions Doc

Mirrors the role of `mku-sales-app_brain.md` in v1 — read this first in any new session working on this repo.

## What this is

V2 of the MKU/MKS sales team PWA, built as a **completely separate project** from `RandyT25/mku-sales-app` (v1, live/production, used daily by ~14 field reps). V2 must never modify v1 or the `MKU-MKS-Area-Dashboard` pipeline repo — those are read-only references.

Live at: **`randyt25.github.io/mku-sales-app-v2`**

Full architecture reasoning and the original milestone spec live in the approved plan this repo was scaffolded from — see git history / ask for the plan doc if context is needed on *why* a decision was made, not just *what*.

## Architecture decisions (confirmed)

- **Backend: Supabase** (Postgres + Auth + Storage) for new write-heavy data — competitor price logs, customer contacts/visit notes, order drafts, canonical customer identity. Chosen over Firebase/Firestore because the milestone-1 features are relational/aggregate-query-shaped (filter by product × area, manager rollups), which Postgres fits naturally and NoSQL fights. Chosen over Cloudflare D1 because Supabase's built-in Auth/Storage/Studio (browser table editor) matter a lot for a solo maintainer who tests exclusively on his phone, not locally.
- **Auth: per-rep PIN.** Pick your name (like v1's dropdown), enter a short PIN. v1 had *zero* real authentication (just a name dropdown + `localStorage` remembering the last selection) — this is the first time the app has real identity, which write-heavy Supabase tables require for RLS.
- **Data visibility: team-wide.** Any rep can see any other rep's competitor logs and visit notes — maximizes intel-sharing value. Management sees everything via an `is_manager` flag on the `reps` table.
- **Offline: writes require a live connection, for now.** No local write-queue/sync in this first backend-backed milestone — revisit only if field connectivity proves to be a real recurring problem.
- **Read-only reference data is NOT duplicated into Postgres.** Targets, stock, SO/order history, and revenue-based customer data keep coming straight from the same Dashboard-repo feed v1 uses (`data_sales.js`, `customers.js`, daily FJ Excel files). Postgres only owns genuinely new data.
- **`products.js` / `toolkit.js` / `announcements.js` are forked once from v1**, not kept in ongoing sync — these are static files local to the v1 app repo (not part of the Dashboard feed), so v2 maintains its own copy independently going forward.

## Phase 0 status: scaffolded, not yet deployed

What exists as of this session:
- Full repo skeleton per the plan (`data/`, `lib/`, `features/`, `styles/`, `supabase/`, `.github/workflows/`).
- Static content forked from v1: `data/products.js`, `data/toolkit.js`, `data/announcements.js`, `data/stock_map.js`, icons, logo, favicon, both catalog PDFs.
- `styles/app.css` — direct 1:1 copy of v1's `sales.css` (design tokens, card pattern, hero header, bottom-nav, bottom-sheet modals all preserved).
- `manifest.json` — adapted for the new scope (`/mku-sales-app-v2/`), icon paths updated to `icons/`.
- `sw.js` — copied verbatim from v1 (deliberately disabled/no-cache — v1 has zero offline story by design, after an earlier caching bug caused stale-data confusion; keeping that behavior until there's a specific reason to change it).
- `index.html` — copied from v1 with **only file-path substitutions** (verified via `diff` — no content lost): CSS/script paths updated to the new `data/`/`styles/`/`lib/` layout, title and login label tagged "V2" so Randy can visually tell the two apps apart when testing both on his phone.

### Deliberate deviation from the original plan's file layout

The plan proposed splitting `sales.js`'s logic into one file per feature under `features/` (`target.js`, `pricelist.js`, `stock.js`, etc.) from the start. **I did not do that split yet.** Instead, `sales.js` (2264 lines) was ported **as a single file, `lib/app-core.js`, unchanged**, and `index.html`'s bootstrap script was updated to load it from its new path.

**Why:** manually re-splitting 2264 lines of interdependent global state and functions in one pass is exactly the kind of mechanical, hard-to-fully-verify transcription work that's easy to introduce a subtle bug in — dropping a function, missing a global reference, breaking an implicit ordering dependency — without a way to catch it except by exhaustively re-testing every screen. Since Phase 0's actual goal is proving the new repo/deploy/manifest setup works and matches v1 exactly, keeping the ported logic as one verified-identical file achieves that goal with far less risk than a big-bang refactor would.

**The plan going forward:** split `app-core.js` into `features/*.js` incrementally, **one module at a time, only when that feature is actually being touched** for a new Supabase-backed capability (e.g., when building the order-builder upgrade in Phase 5, extract just the order-builder logic into `features/order-builder.js` at that point, verify it still works, then move on). This spreads the refactor risk across many small, individually-verifiable steps instead of one large one, and means nothing gets refactored before there's a real reason to touch it.

## Phase 0: DONE — live and verified

- Repo created: `RandyT25/mku-sales-app-v2` (public, matches v1).
- GitHub Pages live at **`https://randyt25.github.io/mku-sales-app-v2/`**.
- Verified in-browser against the live URL (not just local): Target screen (hero %, milestone bars, pace color/tag), Pricelist (348 MKU / 223 MKS products, categories, prices), Stock (live counts: In Stock/Low/Critical/Out), and Toolkit (catalogs, correct page counts) all render correctly with real data from the same Dashboard feed v1 uses.

**Fixed:** v1 and v2 are both hosted on the `randyt25.github.io` origin (different paths), so `localStorage` is shared cross-path there. v2 uses its own `mkuv2_saved_rep` key (not v1's `mku_saved_rep`), so the two apps no longer bleed into each other's last-selected rep.

## Design refresh + SVG icons: DONE

Deepened `--red`/`--blue` tokens, warmed `--bg`/`--surface2`, layered `--shadow-card` across all card components, dual-glow dark hero, and emoji category/status icons replaced with hand-drawn SVGs (`CAT_ICON_PATHS`/`CAT_ICON`/`catIcon()` in `lib/app-core.js`).

## Phase 1: real Supabase PIN auth — code DONE, seeding pending

Uses a **dedicated Supabase project for mku-sales-app-v2** (`hwgrswtahduyqxfokbwj`), not shared with any other app. An earlier draft of this reused Randy's PawonLoka POS project on the theory it was "the same business domain later anyway" — turned out that project's anon key already grants write access to PawonLoka's own cash/stock/asset tables (several have `anon`-scoped RLS policies), which would have exposed unrelated business data the moment this public repo got pushed. Caught before pushing; switched to this project instead. Lesson: don't assume project reuse is safe without checking what RLS policies already grant the anon key.

What exists as of this session:
- `supabase/migrations/0001_create_reps.sql` — `reps` table + RLS (authenticated-read-only; writes are seed-script-only via service_role).
- `lib/rep-alias.js` — shared `deriveLoginAlias()`, used by both the browser and the seed script so alias derivation can't drift.
- `lib/supabase-client.js` — Supabase client via the `esm.sh` CDN build (no bundler).
- `lib/auth.js` — `login`/`logout`/`restoreSession`, exposed as `window.AppAuth` for the classic-script `app-core.js` to call.
- Login UI: picking a name now opens a 6-digit PIN pad (`lib/app-core.js`'s `showPinPad`/`onPinDigit`/`submitPin` etc.) instead of the old "Continue" button. PIN length is 6, not the originally-planned 4, because Supabase enforces a 6-character minimum password length with no dashboard override — rather than padding a 4-digit PIN under the hood, went with a real 6-digit PIN (simpler, no derivation trick, and more brute-force-resistant). A valid Supabase session restores silently on reload (same "land straight on Target" UX as the old fake login); otherwise the PIN pad is required. `selectRep(idx, authInfo)` takes an additive second argument (`repId`/`authUserId`/`isManager`) but is still the single post-login entry point.
- `scripts/seed-reps.mjs` + `scripts/seed-reps.local.mjs.example` — one-time seed script (run locally, service_role key never committed) that creates all 14 reps' Supabase Auth users + `reps` rows. **Run and verified** — all 14 reps seeded successfully; login confirmed working end-to-end on the live site (Monica, PIN `123456`). The other 13 reps are still on the seed script's placeholder PIN `000000` until either they change it (see queued next step below) or Randy sets real ones.
- `is_manager` has no data source yet — the seed script defaults every rep to `false`; flip specific rows in Supabase Studio after seeding.

## Deeper design pass: DONE

Beyond the initial token refresh (Phase 0 section above), a second, more thorough pass landed: hairline borders + tighter shadows on every card (flatter/more "engineered" than pure-shadow separation), tabular numbers everywhere, segmented-control division tabs (Pricelist/Stock), rep avatars (colored circle + initials, using the per-rep color that was assigned but previously unused) in the topbar and both leaderboards, border-radius consolidated to 4 tokens (`--r-sm`/`--r-md`/`--r`/`--r-pill`), consistent press-feedback (`:active` scale) across every interactive control, and a full emoji→SVG sweep (achievement badges, leaderboard medals→plain gold rank numbers + a "YOU" pill, toolkit tabs/module icons, customer-group icons, empty-state icons, stock filters, language-toggle knob) — the earlier Phase 0 emoji cleanup had been less complete than its commit message claimed; this pass finished it.

## Phase 2: competitor price/intel log — DONE, verified live

First real Supabase write-path feature beyond auth. First real use of `features/` (previously empty) — per the incremental-refactor plan above, genuinely new capabilities get their own module there rather than growing `lib/app-core.js` further.

- `supabase/migrations/0003_competitor_logs.sql` — `competitor_logs` table, team-wide read RLS, insert-own-only RLS (append-only for v1 — no edit/delete UI or policy; Randy fixes mistakes directly in Studio for now).
- `features/competitor-intel.js` — `fetchLogs()`/`createLog()`, bridged as `window.CompetitorIntel`.
- New 7th bottom-nav tab "Intel" (eye icon) — search, empty state, a bottom-sheet log-entry form (competitor name/product/price/unit required, category/customer/notes optional), list cards showing rep avatar/name/date.
- `waitForAppAuth()` generalized to `waitForGlobal(key)` so every `window.*` feature bridge shares one readiness-poll helper instead of duplicating it.
- **Verified end-to-end on live Supabase**: logged a real entry, confirmed it persists across reload, confirmed team-wide read and search-filter both work.

## Phase 3: customer identity/dedupe — infrastructure DONE

Investigated first (via Explore agent) rather than assumed: today there are **three customer-identity surfaces that never cross-reference each other** — revenue history keyed by `(rep display name, opaque code)` from the external read-only Dashboard feed, delivery data keyed by free-text `nama_cust` string match, and the Order screen's customer field (free-text, zero validation). `mergeDuplicateCustomers()` (`lib/app-core.js`) already partially compensates for one dimension of this (same-rep duplicate `code`s sharing a name), but nothing reconciles across reps or against the free-text surfaces.

Scope confirmed with Randy as **infrastructure only** — no visible UI change, since nothing consumes it until Phase 4:
- `supabase/migrations/0004_customers.sql` — `customers` table (`name`, `normalized_key` unique, `area`) + `get_or_create_customer(name, area)`, a `security definer` RPC that resolves `upper(trim(name))+upper(trim(area))` to a stable UUID, creating a row the first time a given name+area is seen. Race-safe via `on conflict (normalized_key) do nothing` + re-select.
- `features/customer-identity.js` — `getOrCreateCustomerId()`, bridged as `window.CustomerIdentity`.
- This is now **the** stable FK for all future customer-scoped Supabase data (contacts, visit notes, anything else Phase 4+ needs) — don't invent a second identity scheme later.

## Phase 4: customer contacts + visit notes — DONE, migration applied

- `supabase/migrations/0005_customer_relations.sql` — `customer_contacts` (name/role/phone; team-editable/deletable — reference data, not a log — any rep can fix a stale phone number, not just whoever added it) and `customer_visits` (free-text note; append-only, same pattern as competitor_logs). **Applied by Randy** — live.
- `features/customer-relations.js` — `fetchContacts`/`createContact`/`updateContact`/`deleteContact`/`fetchVisits`/`createVisit`, bridged as `window.CustomerRelations`.
- Both surface inside the existing customer detail modal (`openCustModal()`), resolving `customer_id` via Phase 3's `get_or_create_customer()`.
- **Bug found + fixed during testing**: the new contact/visit forms initially rendered *behind* the already-open customer modal (both shared the same `.overlay` z-index; DOM order decided the tie). Fixed with an explicit higher z-index on the two new form overlays (`#contact-form-overlay`, `#visit-form-overlay`) rather than relying on DOM order.

## Phase 5: order builder upgrade — DONE, verified live

Originally scoped as "reuse previous order, save drafts," but clarifying with Randy revealed the Order screen isn't an order-taking tool — customers send their own PO/WhatsApp with what they want; reps use this screen to build and send a **sales pitch/proposal** (new items to an existing customer, or an opening offer to a new one). Redesigned around that: a gap-analysis suggestion engine (what a customer *isn't* buying, not "recreate what they already buy") plus multiple named local drafts.

- `getPitchSuggestions()` (`lib/app-core.js`) — **real-data bug found and fixed during build**: the original design cross-referenced purchase-history `product.code` against `PRODUCTS.id` and used `product.category`, but production data has an entirely empty `category` field and `code` values are ERP/SO codes (e.g. `"PSJ0044"`) that never match the catalog's own ids (e.g. `"MKU001"`) — the same cross-system identity mismatch documented in Phase 3, showing up again at the product level. Replaced with fuzzy name-prefix matching (`_normProductName`/`_findCatalogMatch`, memoized) against the catalog — verified ~40-50% of purchased line items resolve to a real category across spot-checked customers, enough to reliably infer "categories this customer buys from" (not exact SKU-level novelty, which the data can't support).
- **Design simplified after Randy's review**: the first version had two suggestion buckets — "more from what they buy" and "new categories to try." Randy correctly flagged this as backwards: a customer who already buys a lot of Salmon doesn't need more Salmon suggestions (that relationship is already settled), and the bucket also had a real bug — it never excluded the exact already-bought items, so it could literally re-suggest a product the customer already buys heavily. Removed that bucket entirely; `getPitchSuggestions()` now returns only untapped categories.
- Order screen: `#order-cust` now autocompletes against `getRepCustomerList()` (already handles multi-area reps like Sriasih GT/MT). A recognized customer shows a "Suggested for [name]" panel — categories they've never bought from — one-tap add into the same cart `addToOrder()` already uses. Unrecognized name (new customer) → no panel, unchanged from before.
- Drafts: **local only** (`mkuv2_drafts_<repId>` in localStorage) — personal scratch work, not team-shared data, so no new Supabase table. Multiple named drafts (not just one resume-slot), save/list/load(with-replace-confirm)/delete, a "Drafts (n)" badge button next to the customer field.
- Verified end-to-end: suggestions render correctly against real customer data (no more re-suggesting already-bought items), add-to-cart works, save/load/delete drafts all work, per-rep localStorage namespacing confirmed, no regressions on other tabs.

## Phase 6: "Today" priority screen — DONE, verified live

A new, dedicated bottom-nav tab — **first in the tab order, and the new default landing screen on login**, replacing Target in that role (Target is still fully intact, just one tap away). No new Supabase tables — built entirely from data already loaded plus two new lightweight aggregate reads against Phase 4's existing tables.

- `features/today-priorities.js` — `fetchContactedKeys()`/`fetchLastVisitByKey()` (bridged as `window.TodayPriorities`), each a single full-table read joined to `customers` for the `normalized_key`, rather than one query per customer.
- `normalizedKey(name, area)` (`lib/app-core.js`) — a JS mirror of the SQL formula in `get_or_create_customer()` (`0004_customers.sql`); same "keep in sync manually" convention as `scripts/seed-reps.mjs`'s duplicated `REP_CONFIG`, since the two sides can't share code across the JS/SQL boundary.
- `monthsSinceLastOrder(c)` — extracted from three duplicated inline copies of the same `mAgo` calculation in the Customers screen, now shared with the new inactive-customer signal.
- Seven signal types, each capped to top-5 by revenue: inactive customers, dropped/declining (`getGrowth()` ≤ -15%), untapped pitch opportunities (reuses Phase 5's `getPitchSuggestions()` proactively across all customers instead of only on-demand in Order), no contact on file, no/stale visit notes (>60 days), stock heads-up (cross-references a customer's this-month purchases against live stock via the already-existing `getStockItem()` fuzzy lookup), and unsent draft pitches (Phase 5 localStorage).
- Tapping a customer row opens their detail modal (`openCustModal()`); tapping a pitch-opportunity row jumps straight to the Order screen with that customer prefilled and suggestions showing (`pitchToCustomer()`).
- Verified end-to-end: correct data in every section (spot-checked against Customers/Stock screens directly), both tap-through actions work, logout→login lands on Today, all other tabs unaffected, no console errors.

## Phase 7: management daily recap (v1 — team-wide target performance) — code DONE, pending seed

A genuine, separate **Manager login** — not just flipping `is_manager` on an existing field rep. v1 scope is deliberately just team-wide target performance; more content (aggregate Phase-6-style signals company-wide, a rolled-up competitor intel feed, rep activity/engagement) is queued for later passes, not built now.

- `REP_CONFIG` (`lib/app-core.js`) — 15th entry, `{ name: 'Manager', area: 'Management', div: 'MGMT' }`. `buildLogin()` gives it its own "Management" dropdown optgroup.
- `scripts/seed-reps.mjs` — `Manager` added to the local `REPS` array with `is_manager: true` (an explicit override; `createRepRow()` now takes `is_manager` as a real parameter instead of hardcoding `false` for everyone). **Not yet run for this new entry** — Randy needs to add a `'Manager'` PIN to his local `scripts/seed-reps.local.mjs` and re-run the seed script (safe — skips the 14 existing reps, only creates the new one).
- `renderManagementRecap()` — company-wide Overall % (same Naughty-Nuris-exclusion house rule as the personal Target hero, just summed across every rep instead of one) plus a full team leaderboard and Nestlé team leaderboard, reusing the existing `ac-row`/avatar/bar visual pattern.
- **Known, deliberate v1 limitation**: a Manager login has no personal sales area or customer portfolio, so Today/Order/Customers all show empty/not-meaningful states for this identity. Left as-is for now (verified it doesn't crash anything) — revisit only if it's actually annoying in practice.

### Fix (same day): merged into Target instead of a separate Recap tab

Randy caught this immediately after the v1 above shipped: a Manager login showed **two target-ish screens** — the new Recap tab (correct data) sitting redundantly next to the existing Target tab (broken/empty, since "Manager" has no personal sales area for the personal hero to match against). His call: **merge them, no separate Recap tab at all.**

- `index.html` / `styles/app.css`: `#screen-recap`, `#bnav-recap`, and all `.recap-*` CSS deleted outright. Bottom nav is back to 8 tabs (Today, Target, Pricelist, Order, Stock, Toolkit, Customers, Intel) — one fewer than the v1-above version, which also fits Randy's standing preference for not overcrowding the nav.
- `renderTarget()` (`lib/app-core.js`) now branches on role at the top, same pattern as its existing `isNestleRep()` branch: `if (currentRep.isManager) { renderManagementRecap(); return; }`. Regular reps are completely unaffected.
- `renderManagementRecap()` rewritten to render into the Target screen's own `#target-hero`/`#target-body` elements, using the same dark-hero `.th-*` classes the personal hero uses (`.th-rep` shows "Company Overview" instead of a name) — same data/logic as v1 above, just wired into the shared screen instead of a standalone one.
- `selectRep()`: a Manager login now does `switchTab('target')` (instead of unhiding/switching to the deleted `bnav-recap`) to land on the now-role-aware Target tab rather than the customer-portfolio-scoped Today default.
- Verified end-to-end in-browser (simulated Manager login — real Supabase account still not seeded): regular rep (Monica) Target tab unchanged; Manager login lands on Target showing "Company Overview" dark hero (44%, Rp 3.86B of Rp 8.75B) + both leaderboards; no Recap tab anywhere in the DOM; all 8 tabs cycle with no console errors as Manager.

### Polish pass (same review cycle): Nestlé rollup, gap card, hidden Order tab, search-persist bug

Four more things Randy caught using the merged screen:

- **Nestlé was missing from the Manager's headline %.** `renderManagementRecap()`'s `main_target`/`main_ach` only summed Food+Beverage area targets — the live data feed (`MKU-MKS-Area-Dashboard`'s `data.js`) actually carries `targets: {FOOD, BEVERAGE, NESTLE}` as three peer categories per date, so the Nestlé leaderboard shown on-screen was never reflected in the % above it. Fixed by folding `d.nestle_areas` target/achievement into the same totals — confirmed live: Overall % dropped from 44% (Food+Bev only) to 43% (Food+Bev+Nestlé), total target grew from Rp 8.75B to Rp 10.60B, matching the added Nestlé target.
- **Gap/projection card ported to the Manager view.** The personal Target hero has always had a "How Much More to Go" card (`.gap-card`/`.gc-grid`: to-reach-80%/100%, daily rate needed for each, days remaining + `proj. ~X%`) that the Manager's Company Overview never had. Same card now renders in `renderManagementRecap()` using the company-wide totals (post-Nestlé-fix) and the existing rep-agnostic `daysLeft()`/`timeFactor()` helpers — no new CSS needed, it's all shared classes.
- **Order tab hidden for Manager.** Pricelist and Toolkit are pure reference content and needed no changes for Manager. Order's cart math works fine but its customer-autocomplete/pitch-suggestion panel (`getRepCustomerList()`) will always be empty for a Manager with no personal customer portfolio — rather than ship a half-functional screen, `selectRep()` now toggles `#bnav-order`'s `.hidden` class based on `currentRep.isManager`. Reverts automatically on the next non-Manager login since `selectRep()` re-evaluates it every time.
- **Real bug, unrelated to Manager**: `switchDiv()` (Pricelist) and `switchStockDiv()` (Stock) were wiping the search box every time the MKU/MKS division toggle was tapped, discarding whatever the rep had just typed. Both now leave `plSearch`/`stockSearch` and the input's value alone on toggle — only the division (and Pricelist's category filter, unchanged) reset. Verified live: typed "salmon" on Pricelist, tapped MKS, text and filtered results both persisted (2 MKU matches → 1 MKS match, correctly re-filtered against the new division); same for "chicken" on Stock (12 genuine MKS matches, not an unfiltered list).
- Verified end-to-end in-browser: all four fixes confirmed live via simulated Manager + Monica logins, full 8-tab cycle with no console errors.

### Management completeness audit (same review cycle): unlocked Customers/Today, full hero parity

Randy asked for a full audit of the Manager experience — is everything there, what's missing. Traced every screen `currentRep.isManager` touches against what a regular rep sees.

- **Headline bug found and fixed**: the Customers and Today tabs were completely empty for Manager — not a "no personal portfolio" limitation as previously assumed, but a real bug. `getRepCustomerList()` (`lib/app-core.js`) already had a "return every customer, company-wide" branch, gated on the literal string `rep === 'Management Bali'` — an orphaned login name from a version of the app that predates the current `REP_CONFIG`/`isManager` system. The real Manager login is named `'Manager'`, so it never matched. Fixed by also matching `currentRep?.isManager` in that condition. This one-line fix unlocked both screens for free: Customers now shows the full company-wide, deduped list (1902 customers vs. a regular rep's ~64, still resolving each customer's detail modal via its real owning rep's key), and Today now shows real company-wide inactive/dropped/opportunity/no-contact/stale-visit/stock-alert signals using the same Phase 6 logic, unchanged.
- **VACANT territory mislabeled as an underperforming rep.** The live data has one area (`KUTA - HOTEL`) with `sales: "VACANT"` — an open, unfilled territory — that rendered identically to a real rep in the Team Leaderboard (ranked, avatar, pace-colored). Now detected in `leaderRow()` and rendered muted with an "Unassigned · Open territory" label, excluded from rank/pace coloring.
- **Full hero parity with the personal Target hero**, all ported into `renderManagementRecap()` using company-wide totals in place of one rep's numbers, reusing the same shared CSS/helpers already proven working (`daysLeft()`, `timeFactor()`, `pctColor()`, `pctTag()`, `paceIcon()`, `.th-mini`, `.milestone-card`, `.th-trend`):
  - Data freshness indicator (`● 1d ago` etc.) in the `.th-month` line.
  - Status tag ("On Pace" / "Getting There" / "Push Harder" / "Target Hit!") next to the headline %.
  - Food / Beverage / Nestlé / Naughty Nuris mini-breakdown cards — Naughty Nuris now has visibility company-wide for the first time (still excluded from the main Overall %, per house rule, but no longer invisible).
  - Visual milestone progress bars (`.milestone-card`) for all four categories, with 80%/100% markers, alongside the existing numeric gap card.
  - Monthly trend strip: ported with company-wide monthly aggregation logic, but confirmed **not currently populated for anyone** (Manager or regular reps) — the live data feed's `RAW.targets_by_date` only retains a single (latest) date snapshot, `RAW.dates` doesn't exist at all. This is a pre-existing app-wide limitation, not a regression; the code is correct and will start rendering automatically if the pipeline ever starts retaining multi-month target history.
- Verified end-to-end in-browser: Manager sees 1902 customers / real Today signals / VACANT correctly de-emphasized / full hero parity (43% Overall, "Getting There" tag, 4 mini cards, 4 milestone bars); Monica (regular rep) unaffected — still scoped to her own 64 customers and personal Today signals; full tab cycle with no console errors for both.

### Leaderboard/customers/projection polish (same review cycle)

Three more things Randy caught reviewing the audit build:

- **Naughty Nuris still invisible inside the Team Leaderboard itself** — the aggregate mini-card/milestone-bar from the previous pass wasn't enough; Randy wanted NN tied to the actual reps who own it, matching the pattern the regular-rep Target screen already uses on its own company-wide "Area Leaderboard" (`renderTarget()`'s regular-rep branch): an indented `↳` sub-row under the owning rep, using the pre-existing `.ac-row.nn-subrow` CSS. Ported the same pattern into `renderManagementRecap()`'s Team Leaderboard via a new `nnSubRow()` helper — verified live: `↳ NAUGHTY NURIS (SANUR)` now nests under I Made Luih (49%), `↳ NAUGHTY NURIS (SEMINYAK)` under Sujana (41%).
- **Customer cards didn't show which rep owns them.** Now that Manager's Customers tab merges all 1902 customers company-wide, there was no way to tell whose portfolio a given customer belongs to — the card only showed area/group. The customer detail modal already had this (`cm-meta`); the list cards didn't. Fixed by appending `· ${c.sales}` to `.cust-card-sub`, gated on `currentRep?.isManager` so regular reps' own lists (always their own name) stay unchanged.
- **Projection widget only showed a %, not the amount.** Every "Days remaining / proj. ~X%" readout (Manager's gap card, regular reps' single- and multi-area versions, and the Nestlé rep version — 4 locations total) now also shows the projected Rupiah amount in parens, e.g. `proj. ~96% (Rp 10.11B)`. Same underlying linear-extrapolation math already used for the %, just also formatting the amount via the existing `fmtShort()` instead of discarding it.
- Verified end-to-end in-browser across all 4 rep types (Manager, Monica as single-area, Nestlé rep) plus a full tab cycle with no console errors. One testing note (not a bug): `initCustomers()`'s double-`requestAnimationFrame` defer can stall in an automated/background browser tab — real foreground usage renders within ~2 frames as designed; confirmed by calling `renderCustomers()` directly, which always reflects the current rep instantly.

## Manager account seeded + real login verified

Randy added a real PIN for `'Manager'` (`888888`) to his local `scripts/seed-reps.local.mjs` and the seed script was re-run — confirmed idempotent (all 14 field reps correctly skipped as already-seeded, only `Manager` was newly created: `manager@mkuv2.internal`). This was the first real (non-simulated) Supabase Auth test of the Manager flow.

## Whole-app audit: found `must_change_pin` migration was already live but unbuilt

A broader "what's missing" pass (not scoped to one screen) re-verified the state of the whole app against the live system rather than trusting this doc. Found one real piece of drift: **`supabase/migrations/0002_must_change_pin.sql`'s SQL (the `must_change_pin` column and `mark_pin_changed()` RPC) was already applied directly in Supabase Studio at some point**, contradicting this doc's previous "not yet applied" claim — confirmed via a read-only schema probe against the live REST API (a real column returns `200 []` under RLS-blocked anon access; a genuinely nonexistent column returns PostgREST's `42703` error — `must_change_pin` returned the former). But no code anywhere referenced it — the infrastructure was live and completely inert, meaning 13 of 14 field reps were still sitting on the shared placeholder PIN `000000` with nothing enforcing a change. Everything else audited (v1 feature parity, RLS policies across all 5 migrations, logout, stray TODOs) came back clean — no other gaps found.

## Force PIN change on first login — DONE, verified (mocked flow)

Built immediately given the backend was already live and the gap was real, not hypothetical. No new SQL needed.

- `lib/auth.js`: `REP_SELECT` now includes `must_change_pin`, so both `login()` and `restoreSession()` surface it. New exported `changePin(newPin)` — calls `supabase.auth.updateUser({ password: newPin })` (updates the current session's own password, no service-role needed) then the already-live `mark_pin_changed()` RPC to clear the flag. Registered on `window.AppAuth`.
- `lib/app-core.js`'s PIN pad gets a `_pinMode` state machine (`'login' | 'set' | 'confirm'`) reusing the existing dots/keypad markup, no new HTML: a successful login where `must_change_pin` is true routes into `'set'` (choose a new PIN) → `'confirm'` (re-enter it) instead of calling `selectRep()` immediately. A mismatch on confirm drops back to `'set'` with an error and a cleared buffer; a match calls `AppAuth.changePin()` then proceeds into the app exactly as a normal login would. `onPinBack()` resets all the new state too, so backing out mid-flow doesn't leak into the next attempt.
- **Verified with a mocked `window.AppAuth`** (deliberately not a live Supabase test — this mutates a real rep's actual login password, so didn't do that against a real account without checking with Randy first): confirmed normal wrong-PIN error still works unchanged, a `must_change_pin: true` login correctly routes to set→confirm, a confirm mismatch correctly errors and drops back to set (not through to the app), a matching confirm calls `changePin()` with the right value and lands in the app, a `must_change_pin: false` login is completely unaffected (single-step as before), and `onPinBack()` cleanly resets mid-flow state. No console errors.
- **Important operational note**: every current rep (all 14 field reps + Manager) has `must_change_pin = true` live in the database right now, since nothing has ever cleared it before this feature existed — that includes reps who already have a "real" PIN set (Monica, Manager), not just the 13 still on `000000`. Once this deploys, **every rep will be prompted to choose a new PIN on their very next login**, a one-time app-wide event Randy should be aware of before pushing, not just the 13 reps this was originally framed around.

## UI/data polish from live-device screenshots

Randy sent phone screenshots (logged in as Manager) flagging 6 issues, plus 2 more found while looking closer. All 8 fixed and verified live in-browser:

- **Bottom nav overflow** — a leftover CSS rule (`/* BOTTOM NAV — scrollable for 6 tabs */`) from when the app had 6 tabs was forcing horizontal scroll with fixed-width tabs; now 8 tabs (Today→Intel) don't fit and the last one was clipped off-screen. Deleted the stale override — the still-present base rule (`.bnav { flex:1 }`) now handles all 8 tabs correctly, no scrolling. Primary nav shouldn't scroll on mobile regardless.
- **MKS shown in blue, not red** — `.pl-divtab.active`/`.sdiv.active` (Pricelist and Stock division toggles) both unconditionally used red for whichever division was selected. Added an `.mks` class to both MKS buttons with blue active-state CSS, matching the MKU=red/MKS=blue convention already used elsewhere (`.pl-card-icon.mks` etc.).
- **Toolkit top tabs "cut off" look** — this strip is intentionally horizontally scrollable (5 tabs, icon+label, don't all fit), but the last tab sat flush against the screen edge with no visual hint there was more to scroll to. Added a wrapper div with a right-edge fade-out gradient plus padding, so it now reads as "swipe for more" instead of looking broken.
- **Confusing "Rp 8064.1M" instead of "Rp 8.06B"** — found 7 separate hand-rolled `(v/1000000).toFixed(1)+'M'` formatters scattered across the Customers screen/modal, all predating the already-correct `fmtShort()` helper the rest of the app uses (K → Jt → B tiers). Replaced all 7 with `fmtShort()` calls.
- **Customer modal close** — verified live that tap-outside-to-close *already worked* (no change needed). What was genuinely missing: swipe-down-to-dismiss. Added touch handlers on the modal's existing `.pm-handle` grip — drags past 100px call the existing `closeCustModal()`, shorter drags snap back. First touch-gesture code anywhere in this app.
- **Customers not tied to a real rep were showing** (e.g. "PT. SOEJASCH BALI" under a "Management Bali" bucket). `CUSTOMERS.by_rep` has 4 non-rep aggregation buckets in the source data (`Management Bali`, `Sales Retail`, `Unknown`, `KA` — 56 customers total) that don't correspond to any real `REP_CONFIG` entry. Now skipped in `getRepCustomerList()`'s Manager-only company-wide branch via a hard-coded `NON_REP_CUSTOMER_BUCKETS` skip-list.
  - **Nuance found + resolved with Randy**: some customers (like Soejasch) have *two* records sharing the same customer code — a large one under a junk bucket and a small one under a real rep (Soejasch: Rp 8.06B under "Management Bali" vs. Rp 52.7M under "Redi"). Excluding the junk bucket means that customer still shows, just with the smaller, real-rep-attributed number — not merged, not hidden entirely. Randy's call: **leave it as-is** (show only the real-rep-attributed slice) rather than merging revenue across the bucket boundary or hiding customers with any junk-bucket presence.
- **Order screen customer field used native `<datalist>`** — renders inconsistently on mobile (Android Chrome shows a floating suggestion bar docked above the keyboard instead of an in-page dropdown). Replaced with a custom-built dropdown (`#order-cust-dropdown`): substring-filtered (not just exact-match like the datalist was), capped to top 8 by revenue, closes on outside-tap via a document-level click listener. `populateOrderCustDatalist()` and the `<datalist>` element removed entirely.
- **Catalog "Share" button didn't reliably reach WhatsApp** — `shareCatalog()` used the generic Web Share API (`navigator.share`/`canShare` with a `File`), whose file-attachment support is unreliable across Android Chrome contexts; on failure it silently fell back to a generic OS share-sheet link instead of WhatsApp specifically. Replaced with the same `openWA()` deep-link pattern `shareProductWA()` already used — guaranteed to open WhatsApp with the catalog's public URL prefilled (WhatsApp's `wa.me` protocol can't attach binary files directly, so a link — which WhatsApp auto-unfurls — is the correct working equivalent, not a workaround).
- Verified end-to-end in-browser: all 8 tabs fully visible with zero nav overflow, MKS confirmed blue on both screens, toolkit fade/padding confirmed, revenue formatting confirmed on a real high-value customer, swipe-dismiss confirmed both closing (>100px) and snapping back (<100px), junk-bucket customers confirmed filtered while Monica's own 64-customer list stayed unaffected, Order dropdown confirmed filtering/selecting/outside-close, catalog Share confirmed opening `wa.me` with the right URL. Full 8-tab cycle both as Manager and Monica, no console errors.

## Text sizing, catalog file-share, Intel edit/delete, Manager backoffice pass, live notifications, rep profiles

A large round covering seven things for Randy, three of them brand-new Supabase-backed features. Code is DONE and browser-verified (with mocked Supabase calls, since the new migrations aren't applied yet — see "Action needed" below); real end-to-end testing against live data still needs to happen once Randy applies the migrations.

- **Text too small on phone — fixed.** Root cause: `styles/app.css`'s `/* MOBILE FONT SCALE-DOWN */` block set `:root { font-size: 14px; }` on every phone (≤420px) plus ~28 more shrink overrides on top of that, hitting almost exactly "all the data" (Target hero, leaderboards, gap cards, Pricelist, Order, Stock). Removed the root override and all the data-text overrides; kept only the bottom-nav-specific sizing (still needed so all 8 tabs fit). Verified: root font-size back to 16px, e.g. `.th-pct` now computes to 49.6px instead of the old shrunk ~33.6px.
- **Catalog Share now sends the actual PDF file, not just a link.** Real bug found: `shareCatalog()` was `async` and did `await fetch()` *before* calling `navigator.share()` — browsers can treat the user-activation gesture as expired by the time an async share fires, silently failing file-sharing. Fixed by prefetching both catalog PDFs into an in-memory blob cache (`prefetchCatalogBlobs()`, called once at login) so `navigator.share({files:[file]})` can fire immediately on tap with no fetch delay in between. Falls back to the previous `wa.me` link-share only if file-sharing is genuinely unsupported or fails for a non-cancellation reason. Verified the real 10.6MB MKU catalog PDF prefetches and caches correctly.
- **Competitor intel: edit/delete added.** Was append-only (`0003_competitor_logs.sql`); mistakes had to be fixed in Studio. New migration `0006_competitor_logs_edit.sql` adds update/delete RLS scoped to own entries only (same ownership expression as insert). `features/competitor-intel.js` gained `updateLog()`/`deleteLog()`; the Intel screen's cards now show edit/delete icons only on the signed-in rep's own entries. Verified: edit pre-fills and updates in place, delete removes after confirmation, another rep's entries show no controls at all.
- **Competitor Intel summary view.** New "By Competitor" toggle alongside the existing "Recent" list — groups the already-fetched `_intelLogs` client-side by competitor, showing entry count and price range per product per competitor. No new Supabase query. Verified against 3 mock entries: correctly grouped "PT Sinar Jaya" (2 entries, 2 products) and "CV Berkah" (1 entry).
- **Customer Contacts directory (Manager-only).** Contacts previously only existed one-customer-at-a-time inside `openCustModal()`. New `fetchAllContacts()` in `features/customer-relations.js` (team-wide read RLS already permitted this, no policy change needed) feeds a new searchable directory reachable from a Manager-only button next to the Customers search bar. Tapping a row resolves the contact's customer back to the JS-side `(sales, code)` identity via the shared `normalizedKey()` bridge and opens the real customer modal. Verified: lists across multiple customers, search filters correctly, tap-through opens the right modal and closes the directory.
- **Live Manager notifications — replaces the old hand-edited static file.** The bell-icon announcement panel was fully-built UI (`lib/app-core.js`) reading from a static `data/announcements.js` array Randy had to hand-edit and redeploy to post anything — no way to target one specific rep either. New migration `0007_announcements.sql`: `announcements` table with `author_rep_id`/`target_rep_id` (null = company-wide), RLS visible to everyone (company-wide rows), the target rep (their private memo), or the author (a manager always sees what they sent) — the first policy in this app keyed on matching a column value rather than a flat team-wide/own-only split. Insert restricted to managers only, enforced in the RLS itself. `renderAnnouncements()` now fetches live via new `features/announcements.js` instead of reading the static array; all the existing UI (category colors, unread bell badge, localStorage read-tracking) kept working unchanged. New Manager-only "+ New" compose form with a category and a target picker ("Everyone" or one specific rep). **Bug caught during testing and fixed**: sending a notification while the panel was already open didn't refresh the visible list — `loadAnnouncements()` now always re-renders, not just updates the bell badge. `data/announcements.js` is left in place but unused going forward — Randy's call whether to manually port the old welcome message into the new table.
- **Rep profile view.** Tapping the topbar avatar/name (previously inert) now opens a profile modal: photo upload (new migration `0008_rep_photos.sql` — `reps.photo_url` column + a `rep-photos` Storage bucket, own-photo-only upload policy, public read) and phone number (`reps.phone` already existed, just had no UI) are both editable; a self-service "Change PIN" button reuses `AppAuth.changePin()` behind its own small set→confirm PIN modal (separate from the login screen's state machine, since there's no "login" branch needed once already authenticated). **Name is deliberately not editable** — it's the exact-match key against the external Dashboard feed in ~26 places in `lib/app-core.js`, so a self-service rename would silently sever a rep's link to their own sales history. `avatarHtml()` gained an optional `photoUrl` param (falls back to the initials-circle when absent) — currently wired up for the topbar; not yet propagated to every leaderboard/card call site since only the signed-in rep's own photo is fetched today. Verified: name field present but disabled, phone saves, photo upload updates both the modal preview and the topbar immediately, Change PIN goes through set→confirm→mismatch-retry→success correctly.
- Verified end-to-end in-browser (mocked Supabase globals, since the new migrations aren't live yet): all of the above, plus a full 8-tab cycle as both Manager and Monica with no console errors, and confirmed both new Manager-only entry points (Contacts directory button, notification "+ New" button) are correctly hidden for a regular rep.

### Action needed before any of the three new features work live
1. Apply `supabase/migrations/0006_competitor_logs_edit.sql`, `0007_announcements.sql`, and `0008_rep_photos.sql` in Supabase Studio (same process as every migration so far).
2. `0008` additionally needs a Storage bucket created manually first — bucket creation isn't available through plain SQL migrations: Studio → Storage → New bucket → name it exactly `rep-photos` → Public bucket: on — *then* run `0008`'s policies.
3. Real end-to-end testing against live data (Intel edit/delete, sending a real notification, uploading a real photo) still needs to happen once the above is done — everything so far was verified with mocked Supabase calls, same caveat as every other feature this session before its migration was applied.

### Deferred (discussed with Randy, not building this round)
- **Export — Excel and/or PDF, not CSV.** This app already loads SheetJS's full build (`xlsx.full.min.js`, currently read-only for the FJ feed) — it also supports *writing* `.xlsx`, so Excel export needs no new dependency. PDF would need either a new CDN library or the browser's native print-to-PDF.
- **Photo attachments on competitor intel logs** — "not always we have a proof, but why not." Cheaper once the Storage groundwork from item 7 above exists.
- **Manager-triggered PIN reset for a rep who forgot theirs** — the standard email-reset flow doesn't work (reps log in via a synthetic `.internal` alias, no real inbox). Needs a Supabase Edge Function to use the admin API from a Manager's phone — the first server-side code in this project beyond RLS. Real feature, deliberately deferred.
- Feature 7 (stock-map auto-regen) and the offline write-queue remain queued from earlier passes.
- No automated tests exist anywhere in this project — a deliberate, reasonable tradeoff for a no-build-step solo-maintained PWA tested on-device, not a bug, but worth remembering: nothing catches a regression except manual/simulated re-testing.

## Rules carried over from v1 (still apply)
- Overall % excludes Naughty Nuris (NN) from the denominator — NN shown as a separate mini metric only.
- Pace color thresholds are time-factor-based (`% of month elapsed`), not fixed cutoffs.
- Toolkit content is bilingual `{en, id}` objects — always access via `T()`/`TArr()`, never `.en`/`.id` directly.
- `STOCK_MAP`'s `saldo`/`st`/`buf` fields are stale-by-design — only `code` is load-bearing (bridges product ID → warehouse code). This changes once Feature 7 (auto-regen) lands, but the "only code matters, everything else is a snapshot" contract stays.
- Never touch `/Users/randy/mku-sales-app` or the `MKU-MKS-Area-Dashboard` pipeline repo from this project.
